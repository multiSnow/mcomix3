BUGS
    -Lens is not properly magnifying the right area of the image
     if a rotation has been done.
    -In double page mode, while trying to advance the pages when an archive
     isn't completely loaded yet, MComix often shows the same two pages
     repeatedly.
